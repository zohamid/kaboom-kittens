{
  "name": "kaboom-kittens",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "for file in src/*.js test/*.test.js; do node --check \"$file\" || exit 1; done",
    "test": "node --test",
    "validate": "npm run check && npm test",
    "serve": "python3 -m http.server 8000"
  },
  "engines": {
    "node": ">=18"
  }
}
